[build]
builder = "nixpacks"
# nixpacks.toml pins Node 20 + runs install/build; avoid npm ci EBUSY on cache dir
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "npx serve dist -s -l $PORT"
restartPolicyType = "ON_FAILURE"
